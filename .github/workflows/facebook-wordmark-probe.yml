name: Build Facebook Wordmark Probe

"on":
  workflow_dispatch:
  push:
    paths:
      - "facebook-wordmark-probe/**"
      - ".github/workflows/facebook-wordmark-probe.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    defaults:
      run:
        working-directory: facebook-wordmark-probe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git "$RUNNER_TEMP/theos"

      - name: Install signing tool
        run: |
          brew install ldid

      - name: Build arm64 dylib
        run: |
          set -euo pipefail
          make clean all FINALPACKAGE=1 THEOS="$RUNNER_TEMP/theos"
          mkdir -p artifacts
          dylib_path="$(find .theos -type f -name FacebookWordmarkProbe.dylib -print -quit)"
          test -n "$dylib_path"
          cp "$dylib_path" artifacts/FacebookWordmarkProbe.dylib
          file artifacts/FacebookWordmarkProbe.dylib
          shasum -a 256 artifacts/FacebookWordmarkProbe.dylib > artifacts/SHA256SUMS.txt

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: FacebookWordmarkProbe-arm64
          path: facebook-wordmark-probe/artifacts/
          if-no-files-found: error
