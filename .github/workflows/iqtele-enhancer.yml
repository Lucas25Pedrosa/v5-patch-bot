name: Build iQTele Enhancer

"on":
  workflow_dispatch:
  push:
    paths:
      - "iqtele-enhancer/**"
      - ".github/workflows/iqtele-enhancer.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    defaults:
      run:
        working-directory: iqtele-enhancer

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git "$RUNNER_TEMP/theos"

      - name: Install signing tool
        run: |
          brew install ldid

      - name: Build arm64 dylib
        run: |
          set -euo pipefail
          make clean all FINALPACKAGE=1 THEOS="$RUNNER_TEMP/theos"
          mkdir -p artifacts
          dylib_path="$(find .theos -type f -name iQTeleEnhancer.dylib -print -quit)"
          test -n "$dylib_path"
          cp "$dylib_path" artifacts/iQTeleEnhancer.dylib
          shasum -a 256 artifacts/iQTeleEnhancer.dylib > artifacts/SHA256SUMS.txt

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: iQTeleEnhancer-arm64
          path: iqtele-enhancer/artifacts/
          if-no-files-found: error
