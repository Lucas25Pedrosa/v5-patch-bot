name: Build XLiquidGlass Multi-Account Probe

"on":
  workflow_dispatch:
  push:
    paths:
      - "x-liquid-glass-multiaccount-probe/**"
      - ".github/workflows/x-liquid-glass-multiaccount-probe.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    defaults:
      run:
        working-directory: x-liquid-glass-multiaccount-probe

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Theos
        run: git clone --recursive https://github.com/theos/theos.git "$RUNNER_TEMP/theos"

      - name: Install signing tool
        run: brew install ldid

      - name: Build probe dylib
        run: |
          set -euo pipefail
          make clean all FINALPACKAGE=1 THEOS="$RUNNER_TEMP/theos"
          mkdir -p artifacts
          dylib_path="$(find .theos -type f -name XLiquidGlassMultiAccountProbe.dylib ! -path '*/XLiquidGlassMultiAccountProbe.dylib.dSYM/*' -print -quit)"
          test -n "$dylib_path"
          cp "$dylib_path" artifacts/XLiquidGlassMultiAccountProbe.dylib
          file artifacts/XLiquidGlassMultiAccountProbe.dylib
          shasum -a 256 artifacts/XLiquidGlassMultiAccountProbe.dylib > artifacts/SHA256SUMS.txt

      - name: Upload probe dylib
        uses: actions/upload-artifact@v4
        with:
          name: XLiquidGlassMultiAccountProbe-0.2.1-X12.28.1-arm64
          path: x-liquid-glass-multiaccount-probe/artifacts/
          if-no-files-found: error
